[package]
name = "mail4agent-api"
version = "0.1.0"
edition = "2021"
description = "Wire types for the mail4agent mailbox: addresses, messages, requests and named refusals."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
